{
  "amendmentVersion": "1.23.0",
  "baseContractVersion": "1.22.0",
  "compatibility": "additive",
  "schemas": {
    "TaskPackPublished": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "https://lites.dev/events/TaskPackPublished/1.json",
      "title": "TaskPackPublished v1",
      "type": "object",
      "additionalProperties": false,
      "required": ["event_id", "tenant_id", "user_id", "aggregate_id", "aggregate_version", "occurred_at", "payload"],
      "properties": {
        "event_id": {"type": "string", "format": "uuid"},
        "tenant_id": {"type": "string", "format": "uuid"},
        "user_id": {"type": "string", "format": "uuid"},
        "aggregate_id": {"type": "string", "format": "uuid"},
        "aggregate_version": {"const": 1},
        "occurred_at": {"type": "string", "format": "date-time"},
        "causation_id": {"type": "string"},
        "correlation_id": {"type": "string", "format": "uuid"},
        "payload": {
          "type": "object",
          "additionalProperties": false,
          "required": ["subject_id", "subject_version"],
          "properties": {
            "subject_id": {"type": "string", "format": "uuid"},
            "subject_version": {"const": 1}
          }
        }
      },
      "x-event-type": "TaskPackPublished",
      "x-event-schema-version": 1,
      "x-compatibility": "additive",
      "x-owner": "product-enterprise"
    }
  }
}

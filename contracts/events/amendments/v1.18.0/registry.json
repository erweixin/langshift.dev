{
  "amendmentVersion": "1.18.0",
  "baseContractVersion": "1.17.0",
  "compatibility": "additive",
  "schemas": {
    "ContractProposalCreated": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "https://lites.dev/events/ContractProposalCreated/1.json",
      "title": "ContractProposalCreated v1",
      "type": "object",
      "additionalProperties": false,
      "required": ["event_id", "tenant_id", "user_id", "aggregate_id", "aggregate_version", "occurred_at", "payload"],
      "properties": {
        "event_id": {"type": "string", "format": "uuid"},
        "tenant_id": {"type": "string", "format": "uuid"},
        "user_id": {"type": "string", "format": "uuid"},
        "aggregate_id": {"type": "string", "format": "uuid"},
        "aggregate_version": {"const": 1},
        "occurred_at": {"type": "string", "format": "date-time"},
        "causation_id": {"type": "string"},
        "correlation_id": {"type": "string"},
        "payload": {
          "type": "object",
          "additionalProperties": false,
          "required": ["proposal_id", "action", "target_contract_id", "target_version", "proposal_hash", "reason_ref", "reason_hash", "expires_at"],
          "properties": {
            "proposal_id": {"type": "string", "format": "uuid"},
            "action": {"enum": ["create", "renew", "suspend", "terminate"]},
            "target_contract_id": {"type": "string", "format": "uuid"},
            "target_version": {"type": "integer", "minimum": 0},
            "proposal_hash": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
            "reason_ref": {"type": "string", "minLength": 1, "maxLength": 2048},
            "reason_hash": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
            "expires_at": {"type": "string", "format": "date-time"}
          }
        }
      },
      "x-event-type": "ContractProposalCreated",
      "x-event-schema-version": 1,
      "x-compatibility": "additive",
      "x-owner": "contract-platform"
    },
    "ContractApprovalGranted": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "https://lites.dev/events/ContractApprovalGranted/1.json",
      "title": "ContractApprovalGranted v1",
      "type": "object",
      "additionalProperties": false,
      "required": ["event_id", "tenant_id", "user_id", "aggregate_id", "aggregate_version", "occurred_at", "payload"],
      "properties": {
        "event_id": {"type": "string", "format": "uuid"},
        "tenant_id": {"type": "string", "format": "uuid"},
        "user_id": {"type": "string", "format": "uuid"},
        "aggregate_id": {"type": "string", "format": "uuid"},
        "aggregate_version": {"const": 1},
        "occurred_at": {"type": "string", "format": "date-time"},
        "causation_id": {"type": "string"},
        "correlation_id": {"type": "string"},
        "payload": {"$ref": "#/$defs/contractDecision"}
      },
      "$defs": {
        "contractDecision": {
          "type": "object",
          "additionalProperties": false,
          "required": ["decision_id", "proposal_id", "decision", "proposal_hash", "target_version", "permission_snapshot", "decided_at"],
          "properties": {
            "decision_id": {"type": "string", "format": "uuid"},
            "proposal_id": {"type": "string", "format": "uuid"},
            "decision": {"const": "approve"},
            "proposal_hash": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
            "target_version": {"type": "integer", "minimum": 0},
            "permission_snapshot": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
            "decided_at": {"type": "string", "format": "date-time"}
          }
        }
      },
      "x-event-type": "ContractApprovalGranted",
      "x-event-schema-version": 1,
      "x-compatibility": "additive",
      "x-owner": "contract-platform"
    },
    "ContractApprovalRejected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "https://lites.dev/events/ContractApprovalRejected/1.json",
      "title": "ContractApprovalRejected v1",
      "type": "object",
      "additionalProperties": false,
      "required": ["event_id", "tenant_id", "user_id", "aggregate_id", "aggregate_version", "occurred_at", "payload"],
      "properties": {
        "event_id": {"type": "string", "format": "uuid"},
        "tenant_id": {"type": "string", "format": "uuid"},
        "user_id": {"type": "string", "format": "uuid"},
        "aggregate_id": {"type": "string", "format": "uuid"},
        "aggregate_version": {"const": 1},
        "occurred_at": {"type": "string", "format": "date-time"},
        "causation_id": {"type": "string"},
        "correlation_id": {"type": "string"},
        "payload": {
          "type": "object",
          "additionalProperties": false,
          "required": ["decision_id", "proposal_id", "decision", "proposal_hash", "target_version", "permission_snapshot", "decided_at"],
          "properties": {
            "decision_id": {"type": "string", "format": "uuid"},
            "proposal_id": {"type": "string", "format": "uuid"},
            "decision": {"const": "reject"},
            "proposal_hash": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
            "target_version": {"type": "integer", "minimum": 0},
            "permission_snapshot": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
            "decided_at": {"type": "string", "format": "date-time"}
          }
        }
      },
      "x-event-type": "ContractApprovalRejected",
      "x-event-schema-version": 1,
      "x-compatibility": "additive",
      "x-owner": "contract-platform"
    },
    "ContractProposalExecuted": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "https://lites.dev/events/ContractProposalExecuted/1.json",
      "title": "ContractProposalExecuted v1",
      "type": "object",
      "additionalProperties": false,
      "required": ["event_id", "tenant_id", "user_id", "aggregate_id", "aggregate_version", "occurred_at", "payload"],
      "properties": {
        "event_id": {"type": "string", "format": "uuid"},
        "tenant_id": {"type": "string", "format": "uuid"},
        "user_id": {"type": "string", "format": "uuid"},
        "aggregate_id": {"type": "string", "format": "uuid"},
        "aggregate_version": {"const": 2},
        "occurred_at": {"type": "string", "format": "date-time"},
        "causation_id": {"type": "string"},
        "correlation_id": {"type": "string"},
        "payload": {
          "type": "object",
          "additionalProperties": false,
          "required": ["proposal_id", "contract_id", "contract_version", "contract_status", "proposal_hash", "approval_count", "executed_at"],
          "properties": {
            "proposal_id": {"type": "string", "format": "uuid"},
            "contract_id": {"type": "string", "format": "uuid"},
            "contract_version": {"type": "integer", "minimum": 1},
            "contract_status": {"enum": ["active", "suspended", "terminated"]},
            "proposal_hash": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
            "approval_count": {"const": 2},
            "executed_at": {"type": "string", "format": "date-time"}
          }
        }
      },
      "x-event-type": "ContractProposalExecuted",
      "x-event-schema-version": 1,
      "x-compatibility": "additive",
      "x-owner": "contract-platform"
    },
    "AccountingAdjustmentProposed": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "https://lites.dev/events/AccountingAdjustmentProposed/1.json",
      "title": "AccountingAdjustmentProposed v1",
      "type": "object",
      "additionalProperties": false,
      "required": ["event_id", "tenant_id", "user_id", "aggregate_id", "aggregate_version", "occurred_at", "payload"],
      "properties": {
        "event_id": {"type": "string", "format": "uuid"},
        "tenant_id": {"type": "string", "format": "uuid"},
        "user_id": {"type": "string", "format": "uuid"},
        "aggregate_id": {"type": "string", "format": "uuid"},
        "aggregate_version": {"const": 1},
        "occurred_at": {"type": "string", "format": "date-time"},
        "causation_id": {"type": "string"},
        "correlation_id": {"type": "string"},
        "payload": {
          "type": "object",
          "additionalProperties": false,
          "required": ["proposal_id", "bucket_id", "target_bucket_version", "units", "proposal_hash", "reason_ref", "reason_hash", "expires_at"],
          "properties": {
            "proposal_id": {"type": "string", "format": "uuid"},
            "bucket_id": {"type": "string", "format": "uuid"},
            "target_bucket_version": {"type": "integer", "minimum": 1},
            "units": {"type": "integer", "not": {"const": 0}},
            "proposal_hash": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
            "reason_ref": {"type": "string", "minLength": 1, "maxLength": 2048},
            "reason_hash": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
            "expires_at": {"type": "string", "format": "date-time"}
          }
        }
      },
      "x-event-type": "AccountingAdjustmentProposed",
      "x-event-schema-version": 1,
      "x-compatibility": "additive",
      "x-owner": "contract-platform"
    },
    "AccountingAdjustmentApprovalGranted": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "https://lites.dev/events/AccountingAdjustmentApprovalGranted/1.json",
      "title": "AccountingAdjustmentApprovalGranted v1",
      "type": "object",
      "additionalProperties": false,
      "required": ["event_id", "tenant_id", "user_id", "aggregate_id", "aggregate_version", "occurred_at", "payload"],
      "properties": {
        "event_id": {"type": "string", "format": "uuid"},
        "tenant_id": {"type": "string", "format": "uuid"},
        "user_id": {"type": "string", "format": "uuid"},
        "aggregate_id": {"type": "string", "format": "uuid"},
        "aggregate_version": {"const": 1},
        "occurred_at": {"type": "string", "format": "date-time"},
        "causation_id": {"type": "string"},
        "correlation_id": {"type": "string"},
        "payload": {"$ref": "#/$defs/adjustmentDecision"}
      },
      "$defs": {
        "adjustmentDecision": {
          "type": "object",
          "additionalProperties": false,
          "required": ["decision_id", "proposal_id", "decision", "proposal_hash", "target_bucket_version", "permission_snapshot", "decided_at"],
          "properties": {
            "decision_id": {"type": "string", "format": "uuid"},
            "proposal_id": {"type": "string", "format": "uuid"},
            "decision": {"const": "approve"},
            "proposal_hash": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
            "target_bucket_version": {"type": "integer", "minimum": 1},
            "permission_snapshot": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
            "decided_at": {"type": "string", "format": "date-time"}
          }
        }
      },
      "x-event-type": "AccountingAdjustmentApprovalGranted",
      "x-event-schema-version": 1,
      "x-compatibility": "additive",
      "x-owner": "contract-platform"
    },
    "AccountingAdjustmentApprovalRejected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "https://lites.dev/events/AccountingAdjustmentApprovalRejected/1.json",
      "title": "AccountingAdjustmentApprovalRejected v1",
      "type": "object",
      "additionalProperties": false,
      "required": ["event_id", "tenant_id", "user_id", "aggregate_id", "aggregate_version", "occurred_at", "payload"],
      "properties": {
        "event_id": {"type": "string", "format": "uuid"},
        "tenant_id": {"type": "string", "format": "uuid"},
        "user_id": {"type": "string", "format": "uuid"},
        "aggregate_id": {"type": "string", "format": "uuid"},
        "aggregate_version": {"const": 1},
        "occurred_at": {"type": "string", "format": "date-time"},
        "causation_id": {"type": "string"},
        "correlation_id": {"type": "string"},
        "payload": {
          "type": "object",
          "additionalProperties": false,
          "required": ["decision_id", "proposal_id", "decision", "proposal_hash", "target_bucket_version", "permission_snapshot", "decided_at"],
          "properties": {
            "decision_id": {"type": "string", "format": "uuid"},
            "proposal_id": {"type": "string", "format": "uuid"},
            "decision": {"const": "reject"},
            "proposal_hash": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
            "target_bucket_version": {"type": "integer", "minimum": 1},
            "permission_snapshot": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
            "decided_at": {"type": "string", "format": "date-time"}
          }
        }
      },
      "x-event-type": "AccountingAdjustmentApprovalRejected",
      "x-event-schema-version": 1,
      "x-compatibility": "additive",
      "x-owner": "contract-platform"
    },
    "AccountingAdjustmentExecuted": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "https://lites.dev/events/AccountingAdjustmentExecuted/1.json",
      "title": "AccountingAdjustmentExecuted v1",
      "type": "object",
      "additionalProperties": false,
      "required": ["event_id", "tenant_id", "user_id", "aggregate_id", "aggregate_version", "occurred_at", "payload"],
      "properties": {
        "event_id": {"type": "string", "format": "uuid"},
        "tenant_id": {"type": "string", "format": "uuid"},
        "user_id": {"type": "string", "format": "uuid"},
        "aggregate_id": {"type": "string", "format": "uuid"},
        "aggregate_version": {"const": 2},
        "occurred_at": {"type": "string", "format": "date-time"},
        "causation_id": {"type": "string"},
        "correlation_id": {"type": "string"},
        "payload": {
          "type": "object",
          "additionalProperties": false,
          "required": ["proposal_id", "bucket_id", "bucket_version", "granted_units", "proposal_hash", "approval_count", "executed_at"],
          "properties": {
            "proposal_id": {"type": "string", "format": "uuid"},
            "bucket_id": {"type": "string", "format": "uuid"},
            "bucket_version": {"type": "integer", "minimum": 2},
            "granted_units": {"type": "integer", "minimum": 1},
            "proposal_hash": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
            "approval_count": {"const": 2},
            "executed_at": {"type": "string", "format": "date-time"}
          }
        }
      },
      "x-event-type": "AccountingAdjustmentExecuted",
      "x-event-schema-version": 1,
      "x-compatibility": "additive",
      "x-owner": "contract-platform"
    }
  }
}

---
title: "模块 19：实战项目"
description: "构建实用的 JavaScript 项目，应用所有学到的概念"
---

## 模块 19：实战项目

通过构建实际项目来应用你的 JavaScript 知识。

## 项目想法

### 项目 1：待办事项应用

<UniversalEditor title="待办应用功能">
```javascript !! js
// 功能：
// - 添加、编辑、删除待办事项
// - 标记为完成
// - 按状态过滤
// - 持久化到 localStorage

class TodoApp {
  constructor() {
    this.todos = this.loadTodos();
    this.filter = "all";
    this.render();
  }

  addTodo(text) {
    this.todos.push({
      id: Date.now(),
      text,
      completed: false
    });
    this.saveTodos();
    this.render();
  }

  toggleTodo(id) {
    const todo = this.todos.find(t => t.id === id);
    if (todo) {
      todo.completed = !todo.completed;
      this.saveTodos();
      this.render();
    }
  }

  deleteTodo(id) {
    this.todos = this.todos.filter(t => t.id !== id);
    this.saveTodos();
    this.render();
  }

  saveTodos() {
    localStorage.setItem("todos", JSON.stringify(this.todos));
  }

  loadTodos() {
    const stored = localStorage.getItem("todos");
    return stored ? JSON.parse(stored) : [];
  }

  render() {
    const filtered = this.todos.filter(todo => {
      if (this.filter === "active") return !todo.completed;
      if (this.filter === "completed") return todo.completed;
      return true;
    });

    // 渲染过滤后的待办事项
  }
}
```
</UniversalEditor>

### 项目 2：天气应用

<UniversalEditor title="天气应用">
```javascript !! js
class WeatherApp {
  constructor(apiKey) {
    this.apiKey = apiKey;
    this.baseUrl = "https://api.openweathermap.org/data/2.5";
  }

  async getWeather(city) {
    try {
      const response = await fetch(
        `${this.baseUrl}/weather?q=${city}&appid=${this.apiKey}&units=metric`
      );

      if (!response.ok) {
        throw new Error(`HTTP ${response.status}`);
      }

      return await response.json();
    } catch (error) {
      console.error("获取天气失败:", error);
      throw error;
    }
  }

  async getForecast(city) {
    const response = await fetch(
      `${this.baseUrl}/forecast?q=${city}&appid=${this.apiKey}&units=metric`
    );
    return await response.json();
  }

  displayWeather(data) {
    return {
      city: data.name,
      temp: data.main.temp,
      description: data.weather[0].description,
      icon: data.weather[0].icon
    };
  }
}
```
</UniversalEditor>

### 项目 3：聊天应用

<UniversalEditor title="聊天应用">
```javascript !! js
class ChatApp {
  constructor(wsUrl) {
    this.ws = new WebSocket(wsUrl);
    this.messages = [];
    this.setupWebSocket();
  }

  setupWebSocket() {
    this.ws.onopen = () => {
      console.log("已连接到聊天");
    };

    this.ws.onmessage = (event) => {
      const message = JSON.parse(event.data);
      this.messages.push(message);
      this.renderMessages();
    };

    this.ws.onerror = (error) => {
      console.error("WebSocket 错误:", error);
    };

    this.ws.onclose = () => {
      console.log("已断开聊天连接");
    };
  }

  sendMessage(text) {
    const message = {
      type: "message",
      text,
      timestamp: Date.now(),
      user: this.currentUser
    };

    this.ws.send(JSON.stringify(message));
  }

  renderMessages() {
    // 渲染消息数组
  }
}
```
</UniversalEditor>

## 项目检查清单

<UniversalEditor title="项目检查清单">
```markdown
对于每个项目，确保：
- [ ] 代码结构清晰
- [ ] 错误处理
- [ ] 加载状态
- [ ] 输入验证
- [ ] 响应式设计
- [ ] 可访问性
- [ ] 性能优化
- [ ] 测试
- [ ] 文档
```
</UniversalEditor>

## 最佳实践

<UniversalEditor title="项目最佳实践">
```javascript !! js
// 1. 按模块组织代码
// services/api.js
export class APIClient {
  async get(endpoint) {
    // ...
  }
}

// 2. 使用常量
const CONFIG = {
  API_URL: "https://api.example.com",
  MAX_RETRIES: 3
};

// 3. 错误边界（React）
class ErrorBoundary extends React.Component {
  state = { hasError: false };

  static getDerivedStateFromError(error) {
    return { hasError: true };
  }

  render() {
    if (this.state.hasError) {
      return <h1>出现错误。</h1>;
    }
    return this.props.children;
  }
}

// 4. 验证
function validateEmail(email) {
  const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return regex.test(email);
}
```
</UniversalEditor>

## 总结

### 关键要点

1. **构建实际项目**：应用知识
2. **代码整洁**：组织良好
3. **错误处理**：总是处理错误
4. **测试**：编写测试
5. **文档**：添加注释
6. **迭代**：从简单开始，逐步改进

### 恭喜！

你已完成 Java → JavaScript 学习路径！你现在拥有：
- ✅ 扎实的 JavaScript 基础
- ✅ 现代 ES6+ 语法知识
- ✅ Async/await 掌握
- ✅ DOM 操作技能
- ✅ 框架理解
- ✅ 最佳实践知识

继续练习和构建项目，成为一名熟练的 JavaScript 开发者！

## 学习路径完成

**已完成模块：**
- 模块 0：JavaScript 介绍
- 模块 1：基础语法
- 模块 2：控制流
- 模块 3-4：函数
- 模块 5：数组和集合
- 模块 6-7：对象和类
- 模块 8：原型
- 模块 9：This 上下文
- 模块 10-11：异步编程
- 模块 12-13：DOM 和事件
- 模块 14：模块
- 模块 15：工具
- 模块 16：测试
- 模块 17：框架
- 模块 18：陷阱
- 模块 19：实战项目

**下一步：**
- 构建更多项目
- 学习高级主题
- 探索专业库
- 加入 JavaScript 社区

# Copy to a private, mode-0600 file only for manual operation. The automated
# smoke runner creates random values and secret files outside the repository.
POSTGRES_PASSWORD_FILE=
MINIO_ROOT_USER_FILE=
MINIO_ROOT_PASSWORD_FILE=
GRAFANA_ADMIN_PASSWORD_FILE=
NATS_USER=
NATS_PASSWORD=
VALKEY_PASSWORD=
VAULT_DEV_ROOT_TOKEN=

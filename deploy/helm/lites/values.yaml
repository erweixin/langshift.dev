fullnameOverride: lites

global:
  environment: production
  region: ""
  cell: ""
  version: ""
  imageRegistry: ghcr.io/langshift/lites
  imagePullPolicy: IfNotPresent
  priorityClassName: ""
  traceSampleRatio: "0.10"
  otlpGrpcEndpoint: ""
  otlpTlsServerName: ""
  edgeNamespace: edge-system
  monitoringNamespace: observability
  dnsNamespace: kube-system
  dnsPodLabels:
    k8s-app: kube-dns
  dataNamespace: data-system
  natsPodLabels:
    app.kubernetes.io/name: nats
    app.kubernetes.io/instance: lites-nats
  externalSecretStore:
    kind: ClusterSecretStore
    name: ""
  externalSecretRefreshInterval: 1h

workloads:
  web-app:
    enabled: true
    replicas: 3
    image: {repository: web-app, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: true, port: 3000}
    health: {type: http, port: 3000}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 24, cpuUtilization: 60, memoryUtilization: 70}
    resources:
      requests: {cpu: 250m, memory: 256Mi}
      limits: {cpu: "2", memory: 1Gi}
    requiredEnv: [NODE_EXTRA_CA_CERTS]
    env:
      NODE_EXTRA_CA_CERTS: /run/secrets/api-gateway-ca.crt
    externalEgress: []
  api-gateway:
    enabled: true
    replicas: 3
    image: {repository: api-gateway, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: true, port: 8443}
    health: {type: http, port: 8080}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 12, cpuUtilization: 60, memoryUtilization: 70}
    resources:
      requests: {cpu: 250m, memory: 256Mi}
      limits: {cpu: "2", memory: 1Gi}
    requiredEnv: [DATABASE_URL_FILE, SERVER_TLS_CERT_FILE, SERVER_TLS_KEY_FILE, IDENTITY_UPSTREAM_URL, IDENTITY_UPSTREAM_ROOT_CA_FILE, IDENTITY_UPSTREAM_CLIENT_CERT_FILE, IDENTITY_UPSTREAM_CLIENT_KEY_FILE, IDENTITY_UPSTREAM_TLS_SERVER_NAME, REALTIME_UPSTREAM_URL, REALTIME_UPSTREAM_ROOT_CA_FILE, REALTIME_UPSTREAM_CLIENT_CERT_FILE, REALTIME_UPSTREAM_CLIENT_KEY_FILE, REALTIME_UPSTREAM_TLS_SERVER_NAME, REALTIME_TRUSTED_CONTEXT_AUDIENCE, BEHAVIOR_UPSTREAM_URL, BEHAVIOR_UPSTREAM_ROOT_CA_FILE, BEHAVIOR_UPSTREAM_CLIENT_CERT_FILE, BEHAVIOR_UPSTREAM_CLIENT_KEY_FILE, BEHAVIOR_UPSTREAM_TLS_SERVER_NAME, BEHAVIOR_TRUSTED_CONTEXT_AUDIENCE, AGENT_UPSTREAM_URL, AGENT_UPSTREAM_ROOT_CA_FILE, AGENT_UPSTREAM_CLIENT_CERT_FILE, AGENT_UPSTREAM_CLIENT_KEY_FILE, AGENT_UPSTREAM_TLS_SERVER_NAME, AGENT_TRUSTED_CONTEXT_AUDIENCE, PRODUCT_UPSTREAM_URL, PRODUCT_UPSTREAM_ROOT_CA_FILE, PRODUCT_UPSTREAM_CLIENT_CERT_FILE, PRODUCT_UPSTREAM_CLIENT_KEY_FILE, PRODUCT_UPSTREAM_TLS_SERVER_NAME, PRODUCT_TRUSTED_CONTEXT_AUDIENCE, CONTRACT_UPSTREAM_URL, CONTRACT_UPSTREAM_ROOT_CA_FILE, CONTRACT_UPSTREAM_CLIENT_CERT_FILE, CONTRACT_UPSTREAM_CLIENT_KEY_FILE, CONTRACT_UPSTREAM_TLS_SERVER_NAME, CONTRACT_TRUSTED_CONTEXT_AUDIENCE, GATEWAY_SECRET_BUNDLE_FILE, TRUSTED_CONTEXT_KEYRING_FILE, PUBLIC_ORIGINS, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []
  identity-service:
    enabled: true
    replicas: 3
    image: {repository: identity-service, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: true, port: 8443}
    health: {type: http, port: 8081}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 18, cpuUtilization: 60, memoryUtilization: 70}
    resources:
      requests: {cpu: 500m, memory: 512Mi}
      limits: {cpu: "3", memory: 2Gi}
    requiredEnv: [DATABASE_URL_FILE, SERVER_TLS_CERT_FILE, SERVER_TLS_KEY_FILE, SERVER_CLIENT_CA_FILE, TRUSTED_CONTEXT_KEYRING_FILE, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, IDENTITY_SECRET_BUNDLE_FILE, IDENTITY_PUBLIC_TENANT_ID, ANONYMOUS_HANDLE_KEY_ID, VALKEY_ADDRESSES, VALKEY_PASSWORD_FILE, VALKEY_ROOT_CA_FILE, VAULT_ADDR, VAULT_TOKEN_FILE, VAULT_CACERT, S3_REGION, S3_PAYLOAD_BUCKET, S3_IMPORT_BUCKET, AWS_SHARED_CREDENTIALS_FILE, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []
  realtime-gateway:
    enabled: true
    replicas: 3
    image: {repository: realtime-gateway, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: true, port: 8443}
    health: {type: http, port: 8084}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 36, cpuUtilization: 55, memoryUtilization: 65}
    resources:
      requests: {cpu: 500m, memory: 512Mi}
      limits: {cpu: "3", memory: 2Gi}
    requiredEnv: [DATABASE_URL_FILE, SERVER_TLS_CERT_FILE, SERVER_TLS_KEY_FILE, SERVER_CLIENT_CA_FILE, TRUSTED_CONTEXT_KEYRING_FILE, TRUSTED_CONTEXT_AUDIENCE, NATS_URLS, NATS_CLIENT_CERT_FILE, NATS_CLIENT_KEY_FILE, NATS_ROOT_CA_FILE, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []
  behavior-control-plane:
    enabled: true
    replicas: 3
    image: {repository: behavior-control-plane, digest: ""}
    externalSecretRemoteKey: ""
    service:
      enabled: true
      port: 8443
      additionalPorts: [{name: rollback, port: 8444}]
    health: {type: http, port: 8087}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 12, cpuUtilization: 60, memoryUtilization: 70}
    resources:
      requests: {cpu: 300m, memory: 384Mi}
      limits: {cpu: "2", memory: 1536Mi}
    requiredEnv: [DATABASE_URL_FILE, ADMIN_LISTEN_ADDRESS, ROLLBACK_LISTEN_ADDRESS, SERVER_TLS_CERT_FILE, SERVER_TLS_KEY_FILE, SERVER_CLIENT_CA_FILE, ROLLBACK_ALLOWED_CLIENT_SPIFFE_ID, TRUSTED_CONTEXT_KEYRING_FILE, TRUSTED_CONTEXT_AUDIENCE, BEHAVIOR_SIGNING_KEYRING_FILE, BEHAVIOR_SECRET_BUNDLE_FILE, BEHAVIOR_AUTOMATION_USER_ID, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, VAULT_ADDR, VAULT_TOKEN_FILE, VAULT_CACERT, S3_REGION, S3_PAYLOAD_BUCKET, S3_KMS_KEY_ID, AWS_SHARED_CREDENTIALS_FILE, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []
  agent-control-plane:
    enabled: true
    replicas: 3
    image: {repository: agent-control-plane, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: true, port: 8443}
    health: {type: http, port: 8088}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 24, cpuUtilization: 55, memoryUtilization: 70}
    resources:
      requests: {cpu: 500m, memory: 512Mi}
      limits: {cpu: "3", memory: 2Gi}
    requiredEnv: [DATABASE_URL_FILE, SERVER_TLS_CERT_FILE, SERVER_TLS_KEY_FILE, SERVER_CLIENT_CA_FILE, TRUSTED_CONTEXT_KEYRING_FILE, TRUSTED_CONTEXT_AUDIENCE, AGENT_CONTROL_SECRET_BUNDLE_FILE, CONTROL_BEHAVIOR_ENVIRONMENT, COACH_CONTEXT_MAX_BYTES, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, VAULT_ADDR, VAULT_TOKEN_FILE, VAULT_CACERT, S3_REGION, S3_PAYLOAD_BUCKET, S3_KMS_KEY_ID, AWS_SHARED_CREDENTIALS_FILE, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []
  product-service:
    enabled: true
    replicas: 3
    image: {repository: product-service, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: true, port: 8443}
    health: {type: http, port: 8094}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 24, cpuUtilization: 55, memoryUtilization: 70}
    resources:
      requests: {cpu: 400m, memory: 512Mi}
      limits: {cpu: "3", memory: 2Gi}
    requiredEnv: [DATABASE_URL_FILE, SERVER_TLS_CERT_FILE, SERVER_TLS_KEY_FILE, SERVER_CLIENT_CA_FILE, TRUSTED_CONTEXT_KEYRING_FILE, TRUSTED_CONTEXT_AUDIENCE, PRODUCT_SECRET_BUNDLE_FILE, IDENTITY_PUBLIC_TENANT_ID, PUBLIC_STATUS_DOCUMENT_FILE, PUBLIC_STATUS_KEYRING_FILE, PRODUCT_CONTENT_RELEASE_DIR, ROUTE_BEHAVIOR_ENVIRONMENT, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, VAULT_ADDR, VAULT_TOKEN_FILE, VAULT_CACERT, S3_REGION, S3_PAYLOAD_BUCKET, S3_ARTIFACT_BUCKET, S3_KMS_KEY_ID, AWS_SHARED_CREDENTIALS_FILE, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env:
      EVALUATOR_RUN_TIMEOUT: 10m
      EVALUATOR_RUN_MAX_STEPS: "8"
      EVALUATOR_RUN_MAX_ATTEMPTS: "3"
      EVALUATOR_RUN_MAX_COST_MICROUNITS: "1000000"
    externalEgress: []
  contract-service:
    enabled: true
    replicas: 3
    image: {repository: contract-service, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: true, port: 8443}
    health: {type: http, port: 8096}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 12, cpuUtilization: 55, memoryUtilization: 70}
    resources:
      requests: {cpu: 300m, memory: 384Mi}
      limits: {cpu: "2", memory: 1536Mi}
    requiredEnv: [DATABASE_URL_FILE, SERVER_TLS_CERT_FILE, SERVER_TLS_KEY_FILE, SERVER_CLIENT_CA_FILE, TRUSTED_CONTEXT_KEYRING_FILE, TRUSTED_CONTEXT_AUDIENCE, CONTRACT_SECRET_BUNDLE_FILE, CONTRACT_PROPOSAL_TTL, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, VAULT_ADDR, VAULT_TOKEN_FILE, VAULT_CACERT, S3_REGION, S3_PAYLOAD_BUCKET, S3_KMS_KEY_ID, AWS_SHARED_CREDENTIALS_FILE, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env:
      CONTRACT_PROPOSAL_TTL: 5m
    externalEgress: []
  product-worker:
    enabled: true
    replicas: 3
    image: {repository: product-worker, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: false, port: 0}
    health: {type: http, port: 8095}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 24, cpuUtilization: 60, memoryUtilization: 70}
    resources:
      requests: {cpu: 500m, memory: 512Mi}
      limits: {cpu: "3", memory: 2Gi}
    requiredEnv: [DATABASE_URL_FILE, PRODUCT_SECRET_BUNDLE_FILE, PRODUCT_INBOX_LEASE_PEPPER_FILE, PRODUCT_CONTENT_RELEASE_DIR, ROUTE_BEHAVIOR_ENVIRONMENT, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, NATS_URLS, NATS_CLIENT_CERT_FILE, NATS_CLIENT_KEY_FILE, NATS_ROOT_CA_FILE, VAULT_ADDR, VAULT_TOKEN_FILE, VAULT_CACERT, S3_REGION, S3_PAYLOAD_BUCKET, S3_KMS_KEY_ID, AWS_SHARED_CREDENTIALS_FILE, REMINDER_DELIVERY_URL, REMINDER_DELIVERY_READINESS_URL, REMINDER_DELIVERY_TOKEN_FILE, REMINDER_DELIVERY_ROOT_CA_FILE, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env:
      PORTFOLIO_RECONCILE_INTERVAL: 2s
      PORTFOLIO_RECONCILE_TENANT_BATCH: "500"
      PORTFOLIO_RECONCILE_EXPORT_BATCH: "100"
      PORTFOLIO_EXPORT_RETENTION: 720h
    externalEgress: []
  identity-import-worker:
    enabled: true
    replicas: 3
    image: {repository: identity-import-worker, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: false, port: 0}
    health: {type: http, port: 8082}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 24, cpuUtilization: 65, memoryUtilization: 75}
    resources:
      requests: {cpu: 500m, memory: 512Mi}
      limits: {cpu: "4", memory: 2Gi}
    requiredEnv: [DATABASE_URL_FILE, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, NATS_URLS, NATS_CLIENT_CERT_FILE, NATS_CLIENT_KEY_FILE, NATS_ROOT_CA_FILE, VAULT_ADDR, VAULT_TOKEN_FILE, VAULT_CACERT, S3_REGION, S3_PAYLOAD_BUCKET, S3_IMPORT_BUCKET, AWS_SHARED_CREDENTIALS_FILE, IDENTITY_INBOX_LEASE_PEPPER_FILE, IDENTITY_INVITATION_TOKEN_PEPPER_FILE, CLAIM_IDENTITY_KEY_FILE, IDENTITY_PUBLIC_TENANT_ID, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []
  account-erasure-worker:
    enabled: true
    replicas: 3
    image: {repository: account-erasure-worker, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: false, port: 0}
    health: {type: http, port: 8082}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 12, cpuUtilization: 55, memoryUtilization: 70}
    resources:
      requests: {cpu: 500m, memory: 512Mi}
      limits: {cpu: "3", memory: 2Gi}
    requiredEnv: [DATABASE_URL_FILE, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, NATS_URLS, NATS_CLIENT_CERT_FILE, NATS_CLIENT_KEY_FILE, NATS_ROOT_CA_FILE, VAULT_ADDR, VAULT_TOKEN_FILE, VAULT_CACERT, S3_REGION, S3_PAYLOAD_BUCKET, S3_ARTIFACT_BUCKET, AWS_SHARED_CREDENTIALS_FILE, VALKEY_ADDRESSES, VALKEY_PASSWORD_FILE, VALKEY_ROOT_CA_FILE, ERASURE_INBOX_LEASE_PEPPER_FILE, ERASURE_IDENTITY_KEY_FILE, ERASURE_RECEIPT_KEY_FILE, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env:
      ERASURE_RESTORE_RECONCILE_INTERVAL: 1m
    externalEgress: []
  identity-mail-worker:
    enabled: true
    replicas: 3
    image: {repository: identity-mail-worker, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: false, port: 0}
    health: {type: http, port: 8083}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 18, cpuUtilization: 60, memoryUtilization: 70}
    resources:
      requests: {cpu: 300m, memory: 384Mi}
      limits: {cpu: "2", memory: 1536Mi}
    requiredEnv: [DATABASE_URL_FILE, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, NATS_URLS, NATS_CLIENT_CERT_FILE, NATS_CLIENT_KEY_FILE, NATS_ROOT_CA_FILE, VAULT_ADDR, VAULT_TOKEN_FILE, VAULT_CACERT, S3_REGION, S3_PAYLOAD_BUCKET, AWS_SHARED_CREDENTIALS_FILE, IDENTITY_INBOX_LEASE_PEPPER_FILE, PUBLIC_APP_URL, SMTP_ADDRESS, SMTP_SERVER_NAME, SMTP_FROM_ADDRESS, SMTP_USERNAME, SMTP_PASSWORD_FILE, SMTP_ROOT_CA_FILE, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []
  outbox-publisher:
    enabled: true
    replicas: 3
    image: {repository: outbox-publisher, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: false, port: 0}
    health: {type: http, port: 8081}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 12, cpuUtilization: 60, memoryUtilization: 70}
    resources:
      requests: {cpu: 300m, memory: 256Mi}
      limits: {cpu: "2", memory: 1Gi}
    requiredEnv: [DATABASE_URL_FILE, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, NATS_URLS, NATS_CLIENT_CERT_FILE, NATS_CLIENT_KEY_FILE, NATS_ROOT_CA_FILE, OUTBOX_LEASE_PEPPER_FILE, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []
  agent-scheduler:
    enabled: true
    replicas: 3
    image: {repository: agent-scheduler, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: false, port: 0}
    health: {type: http, port: 8089}
    autoscaling: {enabled: false, minReplicas: 3, maxReplicas: 3, cpuUtilization: 60, memoryUtilization: 70}
    resources:
      requests: {cpu: 500m, memory: 512Mi}
      limits: {cpu: "3", memory: 2Gi}
    requiredEnv: [DATABASE_URL_FILE, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, NATS_URLS, NATS_CLIENT_CERT_FILE, NATS_CLIENT_KEY_FILE, NATS_ROOT_CA_FILE, SCHEDULER_CONFIG_FILE, SCHEDULER_RESOURCE_PEPPER_FILE, SCHEDULER_DISPATCH_PEPPER_FILE, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []
  agent-worker:
    enabled: true
    replicas: 3
    image: {repository: agent-worker, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: false, port: 0}
    health: {type: http, port: 8089}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 48, cpuUtilization: 55, memoryUtilization: 70}
    resources:
      requests: {cpu: 750m, memory: 768Mi}
      limits: {cpu: "4", memory: 3Gi}
    requiredEnv: [DATABASE_URL_FILE, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, NATS_URLS, NATS_CLIENT_CERT_FILE, NATS_CLIENT_KEY_FILE, NATS_ROOT_CA_FILE, NATS_CONSUMER_NAME, PROMPT_ARTIFACT_FILE, PROMPT_ARTIFACT_HASH, MODEL_ROUTE_ARTIFACT_FILE, MODEL_ROUTE_ARTIFACT_HASH, TOOL_REGISTRY_ARTIFACT_FILE, TOOL_REGISTRY_ARTIFACT_HASH, PROVIDER_REGISTRY_FILE, PROVIDER_REGISTRY_FILE_HASH, EXECUTION_ID_KEY_FILE, EXECUTION_LEASE_PEPPER_FILE, LLM_ID_KEY_FILE, LLM_TOKEN_PEPPER_FILE, BILLING_ID_KEY_FILE, AGENT_ID_KEY_FILE, S3_REGION, S3_PAYLOAD_BUCKET, S3_KMS_KEY_ID, AWS_SHARED_CREDENTIALS_FILE, VAULT_ADDR, VAULT_TOKEN_FILE, VAULT_CACERT, VAULT_PROVIDER_SECRET_PREFIX, VAULT_BYOK_SECRET_PREFIX, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []
  tool-worker:
    enabled: true
    replicas: 3
    image: {repository: tool-worker, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: false, port: 0}
    health: {type: http, port: 8090}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 48, cpuUtilization: 55, memoryUtilization: 70}
    resources:
      requests: {cpu: 750m, memory: 768Mi}
      limits: {cpu: "4", memory: 3Gi}
    requiredEnv: [DATABASE_URL_FILE, RUNTIME_DATABASE_URL_FILE, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, NATS_URLS, NATS_CLIENT_CERT_FILE, NATS_CLIENT_KEY_FILE, NATS_ROOT_CA_FILE, NATS_CONSUMER_NAME, TOOL_REGISTRY_ARTIFACT_FILE, TOOL_REGISTRY_ARTIFACT_HASH, EXECUTION_ID_KEY_FILE, EXECUTION_LEASE_PEPPER_FILE, RUNTIME_ID_KEY_FILE, RUNTIME_NONCE_KEY_FILE, RUNTIME_TOKEN_PEPPER_FILE, RUNTIME_PROVISION_DERIVATION_KEY_FILE, RUNTIME_MACHINE_IDENTITY_KEY_FILE, RUNTIME_CAPABILITY_PRIVATE_KEY_FILE, RUNTIME_CAPABILITY_KEY_ID, RUNTIME_HOST_ENDPOINTS_FILE, RUNTIME_HOST_ROOT_CA_FILE, RUNTIME_HOST_CLIENT_CERT_FILE, RUNTIME_HOST_CLIENT_KEY_FILE, RUNTIME_HOST_TLS_SERVER_NAME, S3_REGION, S3_PAYLOAD_BUCKET, S3_ARTIFACT_BUCKET, CLAMAV_ADDRESS, S3_KMS_KEY_ID, AWS_SHARED_CREDENTIALS_FILE, VAULT_ADDR, VAULT_TOKEN_FILE, VAULT_CACERT, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []
  tool-reconciliation-worker:
    enabled: true
    replicas: 3
    image: {repository: tool-reconciliation-worker, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: false, port: 0}
    health: {type: http, port: 8091}
    autoscaling: {enabled: true, minReplicas: 3, maxReplicas: 24, cpuUtilization: 55, memoryUtilization: 70}
    resources:
      requests: {cpu: 500m, memory: 512Mi}
      limits: {cpu: "3", memory: 2Gi}
    requiredEnv: [DATABASE_URL_FILE, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, NATS_URLS, NATS_CLIENT_CERT_FILE, NATS_CLIENT_KEY_FILE, NATS_ROOT_CA_FILE, NATS_CONSUMER_NAME, TOOL_REGISTRY_ARTIFACT_FILE, TOOL_REGISTRY_ARTIFACT_HASH, TOOL_RECONCILIATION_ADAPTERS_FILE, EXECUTION_ID_KEY_FILE, EXECUTION_LEASE_PEPPER_FILE, S3_REGION, S3_PAYLOAD_BUCKET, S3_ARTIFACT_BUCKET, S3_KMS_KEY_ID, AWS_SHARED_CREDENTIALS_FILE, VAULT_ADDR, VAULT_TOKEN_FILE, VAULT_CACERT, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []
  runtime-sweeper:
    enabled: true
    replicas: 3
    image: {repository: runtime-sweeper, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: false, port: 0}
    health: {type: http, port: 8086}
    autoscaling: {enabled: false, minReplicas: 3, maxReplicas: 3, cpuUtilization: 60, memoryUtilization: 70}
    resources:
      requests: {cpu: 300m, memory: 384Mi}
      limits: {cpu: "2", memory: 1Gi}
    requiredEnv: [DATABASE_URL_FILE, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, RUNTIME_ID_KEY_FILE, RUNTIME_TOKEN_PEPPER_FILE, VAULT_ADDR, VAULT_TOKEN_FILE, VAULT_CACERT, S3_REGION, S3_PAYLOAD_BUCKET, S3_KMS_KEY_ID, AWS_SHARED_CREDENTIALS_FILE, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []
  run-cancellation-reconciler:
    enabled: true
    replicas: 3
    image: {repository: run-cancellation-reconciler, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: false, port: 0}
    health: {type: http, port: 8088}
    autoscaling: {enabled: false, minReplicas: 3, maxReplicas: 3, cpuUtilization: 60, memoryUtilization: 70}
    resources:
      requests: {cpu: 500m, memory: 512Mi}
      limits: {cpu: "3", memory: 2Gi}
    requiredEnv: [DATABASE_URL_FILE, RUNTIME_DATABASE_URL_FILE, STORE_EPOCH_URL, STORE_EPOCH_TOKEN_FILE, STORE_EPOCH_ROOT_CA_FILE, EXECUTION_ID_KEY_FILE, EXECUTION_LEASE_PEPPER_FILE, LLM_ID_KEY_FILE, LLM_TOKEN_PEPPER_FILE, BILLING_ID_KEY_FILE, RUNTIME_ID_KEY_FILE, RUNTIME_TOKEN_PEPPER_FILE, VAULT_ADDR, VAULT_TOKEN_FILE, VAULT_CACERT, S3_REGION, S3_PAYLOAD_BUCKET, S3_KMS_KEY_ID, AWS_SHARED_CREDENTIALS_FILE, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []
  store-epoch-authority:
    enabled: true
    replicas: 3
    image: {repository: store-epoch-authority, digest: ""}
    externalSecretRemoteKey: ""
    service: {enabled: true, port: 8443}
    health: {type: tcp, port: 8443}
    autoscaling: {enabled: false, minReplicas: 3, maxReplicas: 3, cpuUtilization: 60, memoryUtilization: 70}
    resources:
      requests: {cpu: 100m, memory: 128Mi}
      limits: {cpu: "1", memory: 512Mi}
    requiredEnv: [STORE_EPOCH_FILE, STORE_EPOCH_BEARER_TOKEN_FILE, SERVER_TLS_CERT_FILE, SERVER_TLS_KEY_FILE, SERVER_CLIENT_CA_FILE, OTLP_ROOT_CA_FILE, OTLP_BEARER_TOKEN_FILE]
    env: {}
    externalEgress: []

migration:
  enabled: true
  image: {repository: lites-migrate, digest: ""}
  existingSecret: ""
  databaseUrlKey: database-url
  activeDeadlineSeconds: 900
  externalEgress: []
